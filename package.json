{
  "name": "contentflow-cms",
  "private": true,
  "scripts": {
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "format": "prettier --write \"{client,server}/**/*.{ts,tsx,js,json,css,md}\"",
    "format:check": "prettier --check \"{client,server}/**/*.{ts,tsx,js,json,css,md}\""
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
